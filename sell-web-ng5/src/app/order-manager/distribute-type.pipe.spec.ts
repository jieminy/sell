import {DistributeTypePipe} from './distribute-type.pipe';

describe('DistributeTypePipe', () => {
  it('create an instance', () => {
    const pipe = new DistributeTypePipe();
    expect(pipe).toBeTruthy();
  });
});
